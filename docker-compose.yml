version: "3.8"
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: appdb
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: password
    ports:
      - "3030:3030"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
